public class Main {
    
    public static void main(String[] args) {
        // Example usage
        LifoQueue queue = new LifoQueue();
        
        // Push elements
        for (int i = 0; i < 100000; i++) {
            queue.push(i);
        }
        
        // Pop elements (LIFO order)
        while (queue.size > 0) {
            System.out.println(queue.pop());
        }
    }
    
    /**
     * Ultra-fast LIFO queue for integers.
     * Optimized for maximum execution speed with minimal overhead.
     * No thread safety, no bounds checking - use carefully.
     */
    static class LifoQueue {
        int[] elements;
        int size;
        
        public LifoQueue() {
            this.elements = new int[16384]; // Pre-allocate for 10k+ elements
            this.size = 0;
        }
        
        public LifoQueue(int initialCapacity) {
            this.elements = new int[initialCapacity];
            this.size = 0;
        }
        
        /**
         * Push element onto queue. Ultra-fast O(1).
         */
        public void push(int value) {
            if (size == elements.length) {
                int[] newElements = new int[size + (size >> 1)];
                System.arraycopy(elements, 0, newElements, 0, size);
                elements = newElements;
            }
            elements[size++] = value;
        }
        
        /**
         * Pop element from queue. Ultra-fast O(1).
         * Returns 0 if empty (no exception for speed).
         */
        public int pop() {
            return elements[--size];
        }
    }
}